{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$comment": "Custom Element Definition",
    "type": "object",
    "properties": {
        "elementName": {
            "description": "Element name displayed in page editor",
            "type": "string"
        },
        "elementTemplateFile": {
            "description": "The template page HTML file. Relative path from templates/elements/",
            "type": "string"
        },
        "enableInput": {
            "description": "What additional input to enable for this element",
            "type": "string",
            "enum": ["collection","embedded","image","gallery"]
        }
    },
    "required": ["elementName","elementTemplateFile"]
}
