{
  "name": "engine",
  "version": "1.0.0",
  "scripts": {
    "prebuild": "rm -rf ./assets/js/dist/*",
    "build": "parcel build ./assets/js/*.js -d ./assets/js/dist --cache-dir ./../../../cache/parcel"
  }
}
